# Implementation Plan

- [ ] 1. Setup Application layer foundation (Jason Taylor style)
  - [ ] 1.1 Create IApplicationDbContext interface in Application/Common/Interfaces
    - Define DbSets for User, OtpVerification, Role
    - Define SaveChangesAsync method
    - _Requirements: 1.1_
  - [ ] 1.2 Create Result and Result<T> classes in Application/Common/Models
    - Success/Failure factory methods, error handling
    - _Requirements: 1.2, 1.3, 1.4, 1.5_
  - [ ] 1.3 Install and configure MediatR in Application project
    - Add MediatR.Extensions.Microsoft.DependencyInjection package
    - _Requirements: 1.1_
  - [ ] 1.4 Install and configure FluentValidation in Application project
    - Add FluentValidation.DependencyInjectionExtensions package
    - _Requirements: 1.2, 1.5_

- [ ] 2. Create OtpVerification entity and update DbContext
  - [ ] 2.1 Create OtpVerification entity in Domain/Entities
    - Add all required properties (Id, Email, Username, PasswordHash, OtpHash, ExpiresAt, AttemptCount, ResendCount, LastResendAt, CreatedAt)
    - _Requirements: 2.1, 2.2, 2.3_
  - [ ] 2.2 Update AppDbContext to implement IApplicationDbContext
    - Add DbSet<OtpVerification>, implement interface
    - _Requirements: 2.1_
  - [ ] 2.3 Configure OtpVerification in OnModelCreating
    - Primary key, unique index on Email
    - _Requirements: 2.1_
  - [ ] 2.4 Create and apply migration for OtpVerification table
    - _Requirements: 2.1_

- [ ] 3. Create RegisterCommand with validation
  - [ ] 3.1 Create RegisterCommand record in Application/Features/Auth/Commands/Register
    - Properties: Email, Username, Password
    - Implement IRequest<Result>
    - _Requirements: 1.1_
  - [ ] 3.2 Create RegisterCommandValidator with FluentValidation
    - Email format validation (regex)
    - Password complexity (8+ chars, uppercase, lowercase, number)
    - _Requirements: 1.2, 1.5_
  - [ ] 3.3 Write property test for email validation
    - **Property 1: Invalid email format rejection**
    - **Validates: Requirements 1.2**
  - [ ] 3.4 Write property test for password validation
    - **Property 2: Password complexity validation**
    - **Validates: Requirements 1.5**

- [ ] 4. Implement OTP utilities
  - [ ] 4.1 Create OtpGenerator static class in Application/Common/Utilities
    - Generate 6-digit random OTP
    - _Requirements: 2.1_
  - [ ] 4.2 Write property test for OTP generation
    - **Property 3: OTP generation format**
    - **Validates: Requirements 2.1**
  - [ ] 4.3 Create OtpHasher static class using BCrypt
    - Hash and Verify methods
    - _Requirements: 2.2_
  - [ ] 4.4 Write property test for OTP hashing
    - **Property 4: OTP storage security**
    - **Validates: Requirements 2.2**

- [ ] 5. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [ ] 6. Implement RegisterCommandHandler
  - [ ] 6.1 Create IEmailService interface in Application/Common/Interfaces
    - SendOtpEmailAsync method
    - _Requirements: 2.4_
  - [ ] 6.2 Create RegisterCommandHandler
    - Inject IApplicationDbContext, IEmailService
    - Check email/username uniqueness via DbContext
    - Generate OTP, hash it, create OtpVerification record
    - Send email via IEmailService
    - _Requirements: 1.1, 1.3, 1.4, 2.1, 2.2, 2.3, 2.5_
  - [ ] 6.3 Write property test for OTP expiration
    - **Property 5: OTP expiration setting**
    - **Validates: Requirements 2.3**

- [ ] 7. Implement VerifyOtpCommand
  - [ ] 7.1 Create VerifyOtpCommand and VerifyOtpCommandValidator
    - Properties: Email, Otp
    - _Requirements: 3.1_
  - [ ] 7.2 Create UserDto in Application/Features/Auth/DTOs
    - _Requirements: 3.1_
  - [ ] 7.3 Create VerifyOtpCommandHandler
    - Get OtpVerification by email
    - Verify OTP hash, check expiration, check attempt count
    - Create User, remove OtpVerification
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_
  - [ ] 7.4 Write property test for valid OTP verification
    - **Property 6: Valid OTP verification creates user**
    - **Validates: Requirements 3.1**
  - [ ] 7.5 Write property test for invalid OTP rejection
    - **Property 7: Invalid OTP rejection**
    - **Validates: Requirements 3.2**
  - [ ] 7.6 Write property test for OTP cleanup
    - **Property 8: OTP cleanup after verification**
    - **Validates: Requirements 3.5, 5.2**

- [ ] 8. Implement ResendOtpCommand
  - [ ] 8.1 Create ResendOtpCommand and ResendOtpCommandValidator
    - Property: Email
    - _Requirements: 4.1_
  - [ ] 8.2 Create ResendOtpCommandHandler
    - Check rate limit (1 minute, max 5 per hour)
    - Invalidate old OTP, generate new one
    - _Requirements: 4.1, 4.2, 4.3_
  - [ ] 8.3 Write property test for OTP invalidation on resend
    - **Property 9: Previous OTP invalidation on resend**
    - **Validates: Requirements 4.2**

- [ ] 9. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [ ] 10. Implement EmailService in Infrastructure
  - [ ] 10.1 Create EmailSettings class for configuration
    - SmtpHost, SmtpPort, SenderEmail, SenderPassword
    - _Requirements: 2.4_
  - [ ] 10.2 Create EmailService implementing IEmailService
    - Gmail SMTP configuration
    - HTML email template for OTP
    - _Requirements: 2.4_

- [ ] 11. Create AuthController in API
  - [ ] 11.1 Create AuthController with ISender injection
    - _Requirements: 1.1_
  - [ ] 11.2 Add POST /api/auth/register endpoint
    - Send RegisterCommand via MediatR
    - _Requirements: 1.1_
  - [ ] 11.3 Add POST /api/auth/verify-otp endpoint
    - Send VerifyOtpCommand via MediatR
    - _Requirements: 3.1_
  - [ ] 11.4 Add POST /api/auth/resend-otp endpoint
    - Send ResendOtpCommand via MediatR
    - _Requirements: 4.1_

- [ ] 12. Configure DI and settings
  - [ ] 12.1 Create DependencyInjection.cs in Application project
    - Register MediatR, FluentValidation, ValidationBehavior
    - _Requirements: 1.1_
  - [ ] 12.2 Create DependencyInjection.cs in Infrastructure project
    - Register AppDbContext as IApplicationDbContext, EmailService
    - _Requirements: 1.1, 2.4_
  - [ ] 12.3 Update Program.cs to use DI extensions
    - AddApplication(), AddInfrastructure()
    - _Requirements: 1.1_
  - [ ] 12.4 Add EmailSettings to appsettings.json
    - SMTP configuration for Gmail
    - _Requirements: 2.4_

- [ ] 13. Final Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
